[package]
name = "axure-downgrade-desktop"
version.workspace = true
description = "Desktop interface for static Axure RP 11 to RP 9 conversion"
authors = ["Axure Downgrade contributors"]
edition.workspace = true
license.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "axure_downgrade_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
axure-core = { path = "../../crates/axure-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

