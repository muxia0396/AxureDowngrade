# Research fixtures

Place locally created fixture pairs in:

```text
fixtures/
  axure9/
  axure11/
  expected/
```

Use the filenames defined in `docs/FORMAT_RESEARCH.md`. Axure source fixtures
may contain proprietary or customer information, so they are ignored by
default until a repository policy is chosen.

